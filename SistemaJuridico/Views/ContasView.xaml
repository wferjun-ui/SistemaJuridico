<UserControl x:Class="SistemaJuridico.Views.ContasView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- NOVA CONTA -->

        <StackPanel Orientation="Horizontal">

            <TextBox Width="120"
                     Margin="5"
                     Text="{Binding NovaConta.Tipo}" />

            <TextBox Width="120"
                     Margin="5"
                     Text="{Binding NovaConta.ValorAlvara}" />

            <TextBox Width="120"
                     Margin="5"
                     Text="{Binding NovaConta.ValorConta}" />

            <Button Content="Adicionar"
                    Margin="5"
                    Command="{Binding AdicionarContaCommand}" />

        </StackPanel>

        <!-- LISTA -->

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Contas}"
                  AutoGenerateColumns="True"/>

    </Grid>
</UserControl>
