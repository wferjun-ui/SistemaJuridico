<UserControl x:Class="SistemaJuridico.Views.VerificacaoSteps.VerificacaoAprovacaoFinalStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <GroupBox Header="Prazos e Notificações">
        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Próximo prazo padrão" Margin="0,0,0,4"/>
                <DatePicker SelectedDateFormat="Short" Text="{Binding Parent.ProximoPrazoPadrao, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="Data da notificação" Margin="0,0,0,4"/>
                <DatePicker SelectedDateFormat="Short" Text="{Binding Parent.DataNotificacao, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </Grid>
    </GroupBox>
</UserControl>
