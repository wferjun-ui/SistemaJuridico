<UserControl x:Class="SistemaJuridico.Views.ProcessoDetalhesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:SistemaJuridico.Views"
             xmlns:local="clr-namespace:SistemaJuridico.Views"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="Background" Value="#4F46E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4F46E5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="4,0"/>
        </Style>

        <Style x:Key="MutedButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <local:QuantidadeFaltanteConverter x:Key="QuantidadeFaltanteConverter"/>
        <Style x:Key="QuantidadeSpinnerButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="12"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="8"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="2,0,0,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="0,8,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360"/>
            <ColumnDefinition Width="14"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="10" Padding="14">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Resumo do Processo" FontSize="17" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="Nº do Processo" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Processo.Numero}" FontSize="15" Margin="0,0,0,8"/>

                    <TextBlock Text="Paciente" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Processo.Paciente}" Margin="0,0,0,6"/>
                    <TextBlock Text="Juiz" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Processo.Juiz}" Margin="0,0,0,6"/>
                    <TextBlock Text="Saldo de contas" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalContasAPrestar, StringFormat=R$ {0:N2}}" Margin="0,0,0,6"/>
                    <TextBlock Text="Última verificação" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding UltimaVerificacaoData}" Margin="0,0,0,6"/>
                    <TextBlock Text="Responsável da última verificação" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding UltimaVerificacaoResponsavel}" Margin="0,0,0,6"/>
                    <TextBlock Text="Status atual" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding StatusAtualResumo}" Margin="0,0,0,6"/>
                    <TextBlock Text="Hash de verificação" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding HashProcesso}" Margin="0,0,0,10"/>

                    <TextBlock Text="Tratamentos cadastrados" FontWeight="Bold" Margin="0,6,0,8"/>
                    <ItemsControl ItemsSource="{Binding ItensSaude}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,10" Padding="10" BorderThickness="1" BorderBrush="#E5E7EB" CornerRadius="8">
                                    <StackPanel>
                                        <TextBlock FontWeight="SemiBold" Text="{Binding Tipo, StringFormat={}{0}: }"/>
                                        <TextBlock Text="{Binding Nome}"/>
                                        <TextBlock Text="{Binding DataPrescricao, StringFormat=Última prescrição: {0}}"/>
                                        <TextBlock Text="{Binding Local, StringFormat=Local: {0}}"/>
                                        <TextBlock Text="{Binding Qtd, StringFormat=Quantidade atual: {0}}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Border Grid.Column="2" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="10" Padding="14">
            <TabControl>
                <TabItem Header="Verificações">
                    <views:VerificacoesView />
                </TabItem>

                <TabItem Header="Prestação de Contas">
                    <views:PrestacaoContasView ProcessoId="{Binding Processo.Id}" />
                </TabItem>

                <TabItem Header="Histórico">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Verificacoes}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,8" Padding="10" BorderThickness="1" BorderBrush="#E5E7EB" CornerRadius="8">
                                        <StackPanel>
                                            <TextBlock Text="{Binding DataHora}" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding StatusProcesso, StringFormat=Status: {0}}"/>
                                            <TextBlock Text="{Binding Responsavel, StringFormat=Responsável: {0}}"/>
                                            <TextBlock Text="{Binding DiligenciaStatus, StringFormat=Diligência: {0}}"/>
                                            <TextBlock Text="{Binding DiligenciaDescricao, StringFormat=Descrição diligência: {0}}" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding PendenciaDescricao, StringFormat=Pendência: {0}}" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding DescricaoPersistente, StringFormat=Descrição persistente: {0}}" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding PrazoDiligencia, StringFormat=Prazo diligência: {0}}"/>
                                            <TextBlock Text="{Binding ProximaVerificacao, StringFormat=Próxima verificação: {0}}"/>
                                            <TextBlock Text="{Binding DataHora, StringFormat=Alterações em: {0:dd/MM/yyyy HH:mm}}" Margin="0,5,0,0" Foreground="#374151"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</UserControl>
