<UserControl x:Class="SistemaJuridico.Views.PrestacaoContasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <GroupBox Header="Prestação de contas" Margin="0,0,0,16">
                <StackPanel Margin="12">
                    <WrapPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Descrição *"/>
                            <TextBox Width="300" Height="32" Text="{Binding ContaSelecionada.Descricao, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Valor *"/>
                            <TextBox Width="140" Height="32" PreviewTextInput="NumericTextInput_PreviewTextInput" Text="{Binding ContaSelecionada.Valor, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, StringFormat=F2}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Data do movimento *"/>
                            <DatePicker Width="150" Height="32" SelectedDate="{Binding ContaSelecionada.DataMovimento, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Loaded="DatePicker_Loaded"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Tipo de movimento"/>
                            <ComboBox Width="170" Height="32" ItemsSource="{Binding TiposMovimento}" SelectedItem="{Binding ContaSelecionada.TipoMovimento}"/>
                        </StackPanel>
                    </WrapPanel>

                    <TextBlock Text="Observações"/>
                    <TextBox Height="70" AcceptsReturn="True" TextWrapping="Wrap" Text="{Binding ContaSelecionada.Observacoes, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Alvará" Margin="0,0,0,16">
                <StackPanel Margin="12">
                    <WrapPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Número do alvará"/>
                            <TextBox Width="200" Height="32" Text="{Binding AlvaraEmEdicao.NumeroAlvara, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Data de expedição"/>
                            <DatePicker Width="150" Height="32" SelectedDate="{Binding AlvaraEmEdicao.DataExpedicao, UpdateSourceTrigger=PropertyChanged}" Loaded="DatePicker_Loaded"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Valor autorizado"/>
                            <TextBox Width="160" Height="32" PreviewTextInput="NumericTextInput_PreviewTextInput" Text="{Binding AlvaraEmEdicao.ValorAutorizado, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Valor recebido"/>
                            <TextBox Width="160" Height="32" PreviewTextInput="NumericTextInput_PreviewTextInput" Text="{Binding AlvaraEmEdicao.ValorRecebido, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Saldo disponível"/>
                            <TextBox Width="160" Height="32" IsReadOnly="True" Text="{Binding AlvaraEmEdicao.SaldoDisponivel, StringFormat=F2}"/>
                        </StackPanel>
                    </WrapPanel>
                    <Button Content="Adicionar alvará" Width="110" Height="30" Command="{Binding AdicionarAlvaraCommand}" HorizontalAlignment="Left"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Tratamentos" Margin="0,0,0,16">
                <StackPanel Margin="12">
                    <WrapPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Descrição do tratamento"/>
                            <TextBox Width="280" Height="32" Text="{Binding TratamentoEmEdicao.DescricaoTratamento, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Quantidade"/>
                            <Grid Width="120" Height="32">
                                <TextBox Padding="0,0,24,0" VerticalContentAlignment="Center" Text="{Binding TratamentoEmEdicao.Quantidade, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <Button Height="16" Width="20" FontSize="9" Content="▲" Click="IncrementarQuantidade_Click"/>
                                    <Button Height="16" Width="20" FontSize="9" Content="▼" Click="DecrementarQuantidade_Click"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Valor unitário"/>
                            <TextBox Width="120" Height="32" PreviewTextInput="NumericTextInput_PreviewTextInput" Text="{Binding TratamentoEmEdicao.ValorUnitario, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, StringFormat=F2}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Valor total"/>
                            <TextBox Width="120" Height="32" IsReadOnly="True" Text="{Binding TratamentoEmEdicao.ValorTotal, StringFormat=F2}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Data início"/>
                            <DatePicker Height="32" SelectedDate="{Binding TratamentoEmEdicao.DataInicio, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Loaded="DatePicker_Loaded"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,8">
                            <TextBlock Text="Data fim"/>
                            <DatePicker Height="32" SelectedDate="{Binding TratamentoEmEdicao.DataFim, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Loaded="DatePicker_Loaded"/>
                        </StackPanel>
                    </WrapPanel>

                    <Button Content="Adicionar tratamento" Width="140" Height="30" Command="{Binding AdicionarTratamentoCommand}" HorizontalAlignment="Left"/>

                    <DataGrid Margin="0,8,0,0" AutoGenerateColumns="False" ItemsSource="{Binding Tratamentos}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Descrição" Binding="{Binding DescricaoTratamento}" Width="*"/>
                            <DataGridTextColumn Header="Quantidade" Binding="{Binding Quantidade}" Width="120"/>
                            <DataGridTextColumn Header="Valor unitário" Binding="{Binding ValorUnitario, StringFormat=F2}" Width="120"/>
                            <DataGridTextColumn Header="Valor total" Binding="{Binding ValorTotal, StringFormat=F2}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Rascunho de contas" Margin="0,0,0,16">
                <StackPanel Margin="12">
                    <TextBlock Text="Alterações permanecem em memória e só entram no histórico ao salvar o rascunho." Margin="0,0,0,8"/>
                    <Button Content="Salvar rascunho" Width="120" Height="30" Command="{Binding SalvarRascunhoCommand}" HorizontalAlignment="Left"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Histórico" Margin="0,0,0,16">
                <DataGrid Margin="12" AutoGenerateColumns="False" ItemsSource="{Binding HistoricoView}" IsReadOnly="True" RowHeight="30">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Data" Binding="{Binding DataEvento, StringFormat=dd/MM/yyyy HH:mm}" Width="180"/>
                        <DataGridTextColumn Header="Evento" Binding="{Binding DescricaoEvento}" Width="220"/>
                        <DataGridTextColumn Header="Usuário" Binding="{Binding Usuario}" Width="150"/>
                        <DataGridTextColumn Header="Detalhes" Binding="{Binding Detalhes}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <WrapPanel HorizontalAlignment="Right">
                <Button Content="Adicionar" Width="110" Height="30" Margin="0,0,8,0" Command="{Binding AdicionarContaCommand}"/>
                <Button Content="Salvar" Width="110" Height="30" Margin="0,0,8,0" Command="{Binding SalvarContaCommand}" IsEnabled="{Binding PodeSalvar}"/>
                <Button Content="Remover" Width="110" Height="30" Margin="0,0,8,0" Command="{Binding RemoverContaCommand}" IsEnabled="{Binding PodeExcluir}"/>
                <Button Content="Salvar rascunho" Width="110" Height="30" Command="{Binding SalvarRascunhoCommand}"/>
            </WrapPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
