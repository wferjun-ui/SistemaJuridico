<Window x:Class="SistemaJuridico.Views.VerificacaoEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SistemaJuridico.ViewModels"
        xmlns:steps="clr-namespace:SistemaJuridico.Views.VerificacaoSteps"
        mc:Ignorable="d"
        Title="Registrar Verificação"
        Height="700"
        Width="1000"
        WindowStartupLocation="CenterOwner"
        WindowState="Maximized">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:VerificacaoDadosGeraisStepViewModel}">
            <steps:VerificacaoDadosGeraisStepView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:VerificacaoAnaliseTecnicaStepViewModel}">
            <steps:VerificacaoAnaliseTecnicaStepView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:VerificacaoFinanceiraStepViewModel}">
            <steps:VerificacaoFinanceiraStepView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:VerificacaoAprovacaoFinalStepViewModel}">
            <steps:VerificacaoAprovacaoFinalStepView/>
        </DataTemplate>
    </Window.Resources>

    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Voltar" Padding="12,6" Margin="0,0,8,0" Command="{Binding VoltarCommand}"/>
            <Button Content="Avançar" Padding="12,6" Margin="0,0,8,0" Command="{Binding AvancarCommand}"/>
            <Button Content="Aprovar" Padding="12,6" Margin="0,0,8,0" FontWeight="Bold" Command="{Binding AprovarCommand}"/>
            <Button Content="Rejeitar" Padding="12,6" Margin="0,0,8,0" Command="{Binding RejeitarCommand}"/>
            <Button Content="Salvar" Padding="12,6" Margin="0,0,8,0" Command="{Binding SalvarCommand}"/>
            <Button Content="Cancelar" Padding="12,6" Click="Cancelar_Click"/>
        </StackPanel>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Background="#F5F8FC" BorderBrush="#D3DCEA" BorderThickness="1" Padding="12" CornerRadius="8" Margin="0,0,0,12">
                    <ItemsControl ItemsSource="{Binding Steps}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                                    <StackPanel>
                                        <Border Width="32" Height="32" CornerRadius="16" BorderThickness="2" BorderBrush="{Binding CorEtapa}" Background="White" HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding Simbolo}" Foreground="{Binding CorEtapa}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                        </Border>
                                        <TextBlock Text="{Binding Nome}" Margin="0,6,0,0" HorizontalAlignment="Center" FontWeight="SemiBold" Foreground="{Binding CorEtapa}"/>
                                    </StackPanel>
                                    <TextBlock Text="➜" Margin="12,0,0,0" VerticalAlignment="Center" FontSize="18" Foreground="{Binding CorEtapa}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TemProximaEtapa}" Value="False">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>

                <ContentControl Content="{Binding CurrentStepViewModel}" Margin="0,0,0,12"/>

                <GroupBox Header="Histórico da Verificação">
                    <DataGrid Margin="8" ItemsSource="{Binding Historico}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Data" Width="170" Binding="{Binding Data, StringFormat=dd/MM/yyyy HH:mm:ss}"/>
                            <DataGridTextColumn Header="Usuário" Width="180" Binding="{Binding Usuario}"/>
                            <DataGridTextColumn Header="Etapa" Width="180" Binding="{Binding Etapa}"/>
                            <DataGridTextColumn Header="Ação" Width="180" Binding="{Binding Acao}"/>
                            <DataGridTextColumn Header="Observação" Width="*" Binding="{Binding Observacao}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
