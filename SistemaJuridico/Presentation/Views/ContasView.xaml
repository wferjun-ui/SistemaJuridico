<UserControl x:Class="SistemaJuridico.Views.ContasView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,3,0,0"/>
            <Setter Property="Padding" Value="8,0"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,3,0,0"/>
            <Setter Property="Padding" Value="8,0"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Background" Value="#4F46E5"/>
            <Setter Property="BorderBrush" Value="#4F46E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Margin="0,0,8,6">
                    <TextBlock Text="Tipo de Registro *" FontWeight="SemiBold"/>
                    <ComboBox SelectedValuePath="Content"
                              SelectedValue="{Binding EdicaoConta.Tipo, UpdateSourceTrigger=PropertyChanged}">
                        <ComboBoxItem Content="Crédito"/>
                        <ComboBoxItem Content="Débito"/>
                        <ComboBoxItem Content="Honorários"/>
                        <ComboBoxItem Content="Custas"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="8,0,8,6">
                    <TextBlock Text="Data da Movimentação *" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding EdicaoConta.DataMovimentacao, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="8,0,0,6">
                    <TextBlock Text="Nº NF/Alvará" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding EdicaoConta.NumNfAlvara, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,0,8,6">
                    <TextBlock Text="Mov. Processo" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding EdicaoConta.MovProcesso, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="1" Margin="8,0,8,6">
                    <TextBlock Text="Valor do Alvará (Crédito)" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding EdicaoConta.ValorAlvara, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Grid.Row="1" Margin="8,0,0,6">
                    <TextBlock Text="Valor da Conta (Débito)" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding EdicaoConta.ValorConta, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Margin="0,0,0,6">
                    <TextBlock Text="Histórico / Detalhes" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding EdicaoConta.Historico, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.ColumnSpan="3">
                    <TextBlock Text="Observações" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding EdicaoConta.Observacao, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Contas}"
                  SelectedItem="{Binding ContaSelecionada}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data" Binding="{Binding DataMovimentacao}" Width="100"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoLancamento}" Width="110"/>
                <DataGridTextColumn Header="Mov. Processo" Binding="{Binding MovProcesso}" Width="110"/>
                <DataGridTextColumn Header="Nº NF/Alvará" Binding="{Binding NumNfAlvara}" Width="110"/>
                <DataGridTextColumn Header="Histórico" Binding="{Binding Historico}" Width="*"/>
                <DataGridTextColumn Header="Crédito" Binding="{Binding ValorAlvara, StringFormat={}{0:N2}}" Width="90"/>
                <DataGridTextColumn Header="Débito" Binding="{Binding ValorConta, StringFormat={}{0:N2}}" Width="90"/>
                <DataGridTextColumn Header="Status" Binding="{Binding StatusConta}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Novo" Command="{Binding NovaContaCommand}"/>
            <Button Content="Salvar Lançamento" Command="{Binding SalvarContaCommand}" Style="{StaticResource PrimaryButton}"/>
            <Button Content="Editar" Command="{Binding EditarContaCommand}"/>
            <Button Content="Excluir" Command="{Binding ExcluirContaCommand}"/>
            <Button Content="Fechar Conta" Command="{Binding FecharContaCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
