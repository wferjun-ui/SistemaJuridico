<UserControl x:Class="SistemaJuridico.Views.MigracaoView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Importação de Dados Históricos"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0 0 0 15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal">

            <TextBox Width="400"
                     Text="{Binding CaminhoArquivo}"
                     Margin="0 0 10 0"/>

            <Button Content="Selecionar"
                    Command="{Binding SelecionarArquivoCommand}"
                    Margin="0 0 10 0"/>

            <Button Content="Importar"
                    Command="{Binding ExecutarMigracaoCommand}"/>

        </StackPanel>

        <TextBox Grid.Row="2"
                 Margin="0 15 0 0"
                 Text="{Binding Relatorio}"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 IsReadOnly="True"/>

    </Grid>
</UserControl>
